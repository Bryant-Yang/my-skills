---
name: interactive-courseware-generator
description: "把单个知识点设计、生成或修复为一页自包含、可操作、可观察、可验证的交互式 HTML 课件。用于用户要求生成一页交互课件、制作 HTML 教学模拟器、产出课件生成提示词，或修改并验证现有交互课件；输入通常包含主题、学习目标、学习者、变量、现象与学科模型。不用于静态幻灯片、整门课程大纲、普通网站、教学平台或只解释源码。"
---

# 交互式课件生成器

## 能力边界

把一个教学概念变成一页可运行的交互式网页。核心合同是：让学生通过调节变量，观察可见现象，比较典型状态，从而理解明确的因果关系。

## 按任务读取资源

- 生成新课件或提示词时读 [生成框架](references/generation-framework.md)。
- 直接构建 HTML，或修改配置、教学动作、宿主桥接时读 [运行协议](references/runtime-contract.md)。
- 生成、修改或验证 HTML 时读 [质量门禁](references/quality-gates.md)。
- PWM 或相近模拟再读 [PWM 示例](references/pwm-example.md)，只借鉴结构，不复制页面设计。
- 迁移到新宿主或判断哪些约束必须保持中性时读 [设计依据](references/design-rationale.md)。

## Workflow

1. 从用户输入提炼一个单页教学目标。信息不足时推断受众、语言和视觉风格；只有不同选择会改变学科结论或课件类型时才提问。
2. 建立“知识点 → 可调变量 → 控件 → 可观察现象 → 对比状态 → 因果结论”映射。每个变量必须有控件和可见结果。
3. 先写课件规格，再按生成框架实现。具体布局、视觉和动画可以创作，但不得改变教学因果关系。
4. 在 HTML 内嵌合法 `courseware-config` 并使用稳定 DOM ID。页面必须能独立运行；需要宿主控制时，再实现运行协议中的四类中性消息。
5. 用户需要教学编排时，基于生成后的真实 DOM 选择器编写 3–8 个讲解/操作步骤；找不到真实目标时用讲解或状态动作，不猜选择器。
6. 对 HTML 运行静态校验器；从本文件位置解析 `<skill-dir>`，不要假设用户当前目录就是 Skill 目录：

   ```bash
   node <skill-dir>/scripts/validate-courseware.js <courseware.html> --json
   ```

   用户明确批准外部依赖时追加 `--allow-external-dependencies`，并在验证报告中逐项列出；相对文件引用也属于外部依赖。

7. 能使用浏览器时，继续检查桌面与移动布局、启动/暂停/重置、每个变量、所有预设、教学动作和控制台错误。修复失败后重新验证。

## Output Contract

- **直接构建**：在用户指定目录输出 `<slug>.html` 和 `<slug>.verification.md`；用户需要讲解编排时再输出 `<slug>.actions.json`。宿主控制只启用 HTML 内的消息桥接，不自动要求动作文件。未指定目录时使用当前工作区的 `courseware/<slug>/`。
- **只要提示词**：输出 `<slug>.prompt.md`，包含教学规格、HTML system/user prompt 和教学动作 prompt，不伪造已生成或已验证的 HTML。
- **修改现有课件**：保留用户已有内容，只修改目标行为，并重新运行相关门禁。

## 硬规则

- 一页只承担一个主要教学目标；交互必须服务于理解，不做装饰。
- 默认生成单文件、自包含 HTML；未经用户允许不依赖外部 CDN、远程脚本、网络接口或特定宿主产品。
- 使用普通代码实现状态机、公式、映射、重置和消息路由；不要用运行时 LLM 代替确定性逻辑。
- 模型自检不等于真实验证。报告必须区分 `已通过`、`警告`、`未检查`。
- 不声称学科公式、视觉效果或跨浏览器行为正确，除非做过对应验证。

## Do Not Use

不要将本 Skill 用于静态幻灯片、整门课程规划、普通网站、教学平台、源码讲解或只需静态信息图的任务；这些请求应交给对应的演示文稿、课程设计、网页开发或分析能力。

## 完成标准

只有同时满足以下条件才称为完成：文件存在；确定性验证无错误；每个变量都有可见因果映射；如生成教学动作则只使用真实选择器；验证报告披露浏览器、视觉、学科、宿主桥接和外部依赖检查状态。

## Package checks

- 路由边界案例见 `evals/trigger_cases.json`。
- 输出合同对比案例见 `evals/output/cases.jsonl`；其中现有结果是 `recorded_fixture`，不能称为模型执行证据。
